spring:
  autoconfigure:
    exclude: org.springframework.cloud.netflix.eureka.server.EurekaServerAutoConfiguration
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

